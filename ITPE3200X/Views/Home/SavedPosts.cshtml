@model List<ITPE3200X.ViewModels.PostViewModel>

@{
    ViewData["Title"] = "Saved Posts";
}

<div class="container">
    @foreach (var post in Model)
    {
        <div id="post-@post.PostId">
            <partial name="~/Views/Home/HomePage.cshtml" model="Model" />
        </div>
    }
</div>